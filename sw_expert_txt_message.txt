hi ryan dahl, 

i have this problem -- 

REGION SELECTION COMPLETELY NON-FUNCTIONAL IN CHROME EXTENSION

I'm building a Chrome extension (Manifest V3) with region selection functionality. When user clicks "Select Region" button in popup, the canvas is created successfully but selection mode never activates. No mouse events are captured for drawing rectangles.

CRITICAL FINDING: The popup button click is never sending the startSelection message to content script. Popup debugging shows NO LOGS when button is clicked, indicating the click handler isn't firing.

here is all the relevant code you need to know -- 

POPUP.HTML - Button Definition:
```html
<div id="regionControls">
  <button class="primary-btn" id="selectRegionBtn">Select Region</button>
  <button class="secondary-btn" id="clearRegionsBtn" disabled>Clear All</button>
  <button class="primary-btn" id="toggleRegionBlurBtn" disabled>Activate Blur</button>
</div>
```

POPUP.JS - Event Handler Setup:
```javascript
// Region controls
document.getElementById('selectRegionBtn').addEventListener('click', () => {
  console.log('🔍 POPUP: Select Region button clicked');  // THIS NEVER APPEARS
  sendMessage('startSelection');
  console.log('🔍 POPUP: startSelection message sent, closing popup');
  window.close();
});

function sendMessage(action, data = {}) {
  console.log('🔍 POPUP: Sending message:', action, data);  // THIS NEVER APPEARS
  chrome.tabs.query({ active: true, currentWindow: true }, (tabs) => {
    console.log('🔍 POPUP: Active tab found:', tabs[0]?.id);
    chrome.tabs.sendMessage(tabs[0].id, { action, ...data }, (response) => {
      if (chrome.runtime.lastError) {
        console.error('🔍 POPUP: Error sending message:', chrome.runtime.lastError.message);
      } else if (response) {
        console.log('🔍 POPUP: Response received:', response);
        refreshState();
      } else {
        console.log('🔍 POPUP: No response received');
      }
    });
  });
}

// --- Initialize ---
document.addEventListener('DOMContentLoaded', () => {
  console.log('🔍 POPUP: DOM loaded, checking elements');
  console.log('🔍 POPUP: selectRegionBtn exists:', !!document.getElementById('selectRegionBtn'));
  refreshState();
});
```

CONTENT.JS - Message Handler (Working):
```javascript
chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
  debugLog('SYSTEM', 'Content script received message', message);
  
  if (message.action === 'startSelection') {
    startRegionSelection();  // THIS IS NEVER CALLED
    sendResponse({ success: true });
  }
  // ... other handlers work fine
  
  return true;
});

function startRegionSelection() {
  debugLog('STATE', 'Starting region selection mode');  // THIS LOG NEVER APPEARS
  // ... canvas interaction setup
}
```

POPUP UI STRUCTURE:
```html
<!-- Mode Selector -->
<div class="mode-selector">
  <button class="mode-btn active" id="fullPageMode">Full Page</button>
  <button class="mode-btn" id="regionsMode">Regions</button>
</div>

<!-- Full Page Controls -->
<div id="fullPageControls">
  <button class="primary-btn" id="toggleBlurBtn">Toggle Blur</button>
</div>

<!-- Region Controls -->
<div id="regionControls" style="display: none;">
  <button class="primary-btn" id="selectRegionBtn">Select Region</button>
  <!-- ... other buttons -->
</div>
```

POPUP UI SWITCHING LOGIC:
```javascript
// Show/hide appropriate controls
document.getElementById('fullPageControls').style.display = state.mode === 'full-page' ? 'block' : 'none';
document.getElementById('regionControls').style.display = state.mode === 'regions' ? 'block' : 'none';
```

here is what i have tested --- 

1. POPUP INITIALIZATION LOGS (these appear correctly):
```
🔍 POPUP: DOM loaded, checking elements
🔍 POPUP: selectRegionBtn exists: true
```

2. MODE SWITCHING WORKS (Regions mode activated):
```
🎯 [04:03:11.238] [SYSTEM] Content script received message {action: 'setMode', mode: 'regions'}
```

3. CANVAS CREATION WORKS (in regions mode):
```
🎯 [04:03:11.238] [CANVAS] createCanvasOverlay() called
🎯 [04:03:11.239] [CANVAS] Canvas overlay created successfully
```

4. BUTTON CLICK DEBUGGING (COMPLETELY MISSING):
When I click "Select Region" button, NO popup console logs appear:
- Missing: "🔍 POPUP: Select Region button clicked"
- Missing: "🔍 POPUP: Sending message: startSelection"
- Missing: All sendMessage logs

5. CONTENT SCRIPT NEVER RECEIVES MESSAGE:
No logs for: "Content script received message {action: 'startSelection'}"

6. OTHER BUTTONS WORK FINE:
- Full Page/Regions mode switching works
- Toggle blur button works  
- All other popup buttons trigger console logs correctly

7. ELEMENT VERIFICATION:
- selectRegionBtn element exists (confirmed via DOM inspection)
- Element is visible when in regions mode
- No JavaScript errors in console
- Button appears clickable in UI

WORKING VS BROKEN COMPARISON:

WORKING (toggle blur button):
```javascript
document.getElementById('toggleBlurBtn').addEventListener('click', () => {
  sendMessage('toggleBlur');  // This triggers logs and works
});
```

BROKEN (select region button):
```javascript
document.getElementById('selectRegionBtn').addEventListener('click', () => {
  console.log('🔍 POPUP: Select Region button clicked');  // This never appears
  sendMessage('startSelection');
});
```

I need you to suggest very clearly what is the issue, why the selectRegionBtn click handler isn't firing when other identical button handlers work fine, and suggest the fix for this specific event handler attachment problem.

The canvas/content script logic is working perfectly - this is purely a popup button click handler registration issue.